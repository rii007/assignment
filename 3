#include <stdio.h>
int fibo(int n){
    int i,a=0,b=1,c;
    for(i=2;i<=n;i++){
    c=a+b;
    a=b;
    b=c;
    }
    return a;
}
int main(){
    int  term,result;
    printf("enter term:-");
    scanf("%d",&term);
    result=fibo(term);
    
    printf("the term is: %d",result);
    return 0;
}

